<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Фон элемента -->
    <solid android:color="#1C1C1E" /> <!-- Тёмный фон в стиле Сбера -->

    <!-- Скруглённые углы -->
    <corners android:radius="12dp" />

    <!-- Отступы внутри элемента -->
    <padding
        android:left="16dp"
        android:right="16dp"
        android:top="12dp"
        android:bottom="12dp" />
</shape>
